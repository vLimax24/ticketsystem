import React from 'react'
import { TicketCardLayout, CardForm } from './index'

const TicketCard = () => {
  return (
    <TicketCardLayout>
        <CardForm />
    </TicketCardLayout>
  )
}

export default TicketCard